import React from "react"
import { gsap } from "gsap"
import { cssRulePlugin } from "gsap"

//ToDo for dm/lm toggle
//create toggler component
// set default state light mode
// set state for enabled, when enabled, be dark mode

export function modeToggler(props) {
  function toggleMode() {}
  return (
    <div>
      <div onClick={{}}></div>
    </div>
  )
}
